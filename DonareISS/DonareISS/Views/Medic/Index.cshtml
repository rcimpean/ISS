
@model DonareISS.Medic

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutMedic.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Spital)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Pacient)
        </th>
        <th></th>
    </tr>
    <tr>
        <td>
            @Html.DisplayFor(model => model.Spital)
        </td>
        <td>
            @foreach (var it in Model.Pacient)
            {
                @Html.DisplayFor(modlIt => it.Id_Pacient)
            }
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = Model.Id_Medic }) |
            @Html.ActionLink("Details", "Details", new { id = Model.Id_Medic }) |
            @Html.ActionLink("Delete", "Delete", new { id = Model.Id_Medic }) |
            @Html.ActionLink("Pacienti", "Pacienti", new { id = Model.Id_Medic })
        </td>
    </tr>

</table>
