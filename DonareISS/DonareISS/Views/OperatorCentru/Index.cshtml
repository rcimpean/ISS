@model IEnumerable<DonareISS.OperatorCentru>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Adresa)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Adresa)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_Operator }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_Operator }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_Operator })
        </td>
    </tr>
}

</table>
@Html.Action("CerereNoua")